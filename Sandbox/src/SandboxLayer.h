#pragma once
#include <Ongine.h>
class SandboxLayer : public ON::Layer {
public:
	virtual void OnRun() override {
	}
	virtual void OnUpdate(ON::Input& input) override {
	}
	virtual void OnRender() override {
	}
};